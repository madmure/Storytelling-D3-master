// Set a graph with % per column

	// Link graph to data : categories + gender + values in %

	// Draw the graph according to data

		// Draw the X Y axis
		// Draw bars for each category with the values
		// The bars should be fils with 'phone' svg.